extends ../layout

block content
  form(method="post" enctype="multipart/form-data")
    div
      label(for="b_write") 작성자
      input(type="text", name="b_write")#b_write
    div
      label(for="b_subject") 제목
      input(type="text" name="b_subject")#b_subject
    div
      label(for="b_content") 내용
      input(type="text" name="b_content")#b_content
    div
      label(for="b_upfile") 파일
      input(type="file" name="b_upfile")#b_upfile
    div
      label(for="") 
      input(type="SUBMIT", value="저장")